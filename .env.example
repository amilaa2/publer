# Copy to .env for local dev. On Vercel, add these in Project → Settings → Environment Variables.

# Demo mode: uses bundled sample data (recommended for this frontend-only deploy)
VITE_DEMO_MODE=true

# Backend API base URL (only used when VITE_DEMO_MODE=false)
VITE_API_BASE_URL=http://localhost:8000
